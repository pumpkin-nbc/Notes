# 安装watchtower

```shell
# 每隔3600秒检测一次 并更新 将旧镜像删除
sudo docker run -d --name watchtower --restart=always -v /etc/localtime:/etc/localtime -v /var/run/docker.sock:/var/run/docker.sock containrrr/watchtower --cleanup --include-stopped --interval 3600
```

