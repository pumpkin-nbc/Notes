# 安装Postgres

```shell
# 安装 把数据存到本地 mysecretpassword替换为数据库密码 默认账户为 postgres
docker run -d --name postgres - -e POSTGRES_PASSWORD=mysecretpassword -v /data/PostgreSQL/data:/var/lib/postgresql/data -v /etc/localtime:/etc/localtime postgres
```

